# Next work (recommended)

Prioritize in order unless the user redirects. Keep Severance voice + continuity files in sync when adding features.

## Recently shipped

- **Floor campus** — `?room=` router, hallway/wellness/breakroom/od/perpetuity scenes, desk peeks, ESC return
- **Departments + sidequests** — standing readout in UTILITIES; starter invitations; ambient can issue quests
- **Engine v3 migrate** — `campus` / `departments` / `sidequests` beside protocol bins
- Engine v2 protocol bins, MDR frame UI, island landing, QOL (prior)

## High value next slices

### 1. Phone equal polish
- Bin micro-labels; A2HS timing
- ~~Rail toggles UX~~ (TEMPERS/VITALS titlebar fly-outs)

### 2. Snapshot golden baselines
- Commit Playwright screenshot baselines if CI needs them

### 3. Temper digit deepening
- More dominant-temper digit behaviors tied to bin shortfall micros

## Recently shipped (looks)

- **Rooms ↔ gameplay looks** — `campusLooks.js` drives temper/pressure/standing/`quest-beacon` CSS from existing state

## Lumon / Severance alignment (ongoing)

When touching UI/copy/ambient/rooms:

- Re-read `severance-facts.md` + `show-app-continuity.md` + `visual-direction.md` + `floor-campus.md`
- Habit math → `engine.js` only
- Ambient stays hours-scale; Perpetuity never daily default

## Explicitly out of scope (unless asked)

- Framework migration
- Frequent ambient (sub-5-minute surprises)
- Auto full MDE / Waffle from ambient
- Shipping copyrighted Severance / show audio
- Committing secrets / `.git-status.txt` / `proxmox_mcp.log`
