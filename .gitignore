# Dependencies
node_modules/

# Test artifacts
test-results/
playwright-report/

# Archival server data
server/data/

# Local-only secrets / lab notes (keep README so the folder is seeded)
.secrets/*
!.secrets/README.md

# Local deploy artifacts (Proxmox / CapRover tarball)
deploy.tar
.build-id

# OS
.DS_Store
Thumbs.db
